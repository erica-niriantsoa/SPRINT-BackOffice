<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/data/jpa https://www.springframework.org/schema/data/jpa/spring-jpa.xsd">

    <!-- Enable component scanning and auto-configuration -->
    <context:component-scan base-package="com.backoffice" />
    <context:annotation-config/>

    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer">
        <property name="ignoreUnresolvablePlaceholders" value="true"/>
    </bean>

     <!-- Définition de la source de données PostgreSQL -->
    <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="org.postgresql.Driver" />
        <!-- Utilise DatabaseUrlParser pour convertir l'URL Render -->
        <property name="url" value="#{T(com.backoffice.config.DatabaseUrlParser).getJdbcUrl()}" />
        
        <!-- Configuration du pool de connexions -->
        <property name="initialSize" value="2" />
        <property name="maxTotal" value="10" />
        <property name="maxIdle" value="5" />
        <property name="minIdle" value="2" />
    </bean>

     <!-- Entity Manager Factory avec Hibernate -->
    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="packagesToScan" value="com.backoffice.entity" />
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter" />
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</prop>
                <prop key="hibernate.hbm2ddl.auto">update</prop>
                <prop key="hibernate.show_sql">false</prop>
                <prop key="hibernate.format_sql">true</prop>
            </props>
        </property>
    </bean>

     <!-- Gestionnaire de transactions -->
    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory" />
    </bean>

    <!-- Activation de l'annotation @Transactional -->
    <tx:annotation-driven transaction-manager="transactionManager" />

    <!-- Activer Spring Data JPA -->
    <jpa:repositories base-package="com.backoffice.repository" />
</beans>
```

## Changements effectués :

1. ✅ `base-package="com.backoffice"` - Scan tous les sous-packages
2. ✅ `T(com.backoffice.config.DatabaseUrlParser)` - Chemin complet
3. ✅ `packagesToScan="com.backoffice.entity"` - Pour les entités JPA
4. ✅ `jpa:repositories base-package="com.backoffice.repository"` - Pour les repositories

---

## ✅ Configuration Render - C'est BON !

Votre variable d'environnement sur Render est **correcte** :
```
DATABASE_URL = postgresql://sprint_user1:US8l9xVpuJQ7XvadIkNAHpLctma3YAyF@dpg-d5ugoha4d50c73d65tv0-a/sprint_db1
```

⚠️ **Mais vérifiez qu'il manque le domaine complet !** L'URL devrait être :
```
DATABASE_URL = postgresql://sprint_user1:US8l9xVpuJQ7XvadIkNAHpLctma3YAyF@dpg-d5ugoha4d50c73d65tv0-a.oregon-postgres.render.com/sprint_db1
```

Copiez l'**Internal Database URL** exacte depuis votre PostgreSQL Dashboard → **Connections**.

---

## Structure finale requise :
```
src/main/
├── java/
│   └── com/
│       └── backoffice/
│           ├── config/
│           │   └── DatabaseUrlParser.java
│           ├── controller/
│           │   └── TestDbServlet.java
│           ├── service/
│           ├── repository/
│           └── entity/
├── resources/
│   └── app-context.xml (le fichier corrigé ci-dessus)
└── webapp/
    └── index.html